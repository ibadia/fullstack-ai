Django==4.2.15

gunicorn==22.0.0
uvicorn==0.23.2

# for sentry error monitoring
sentry-sdk>=2.8.0

# for creating REST APIs
djangorestframework==3.15.2

# for swagger doc
drf-yasg==1.21.8

# to support json field
django-jsonfield==1.4.1

# token
djangorestframework-simplejwt==5.3.1

# for CORS
django-cors-headers==4.3.1

# for testing
pytest==7.4.0
pytest-django==4.5.2
factory_boy==3.2.1
pytest-cov==4.1.0


# for code formatting
black==22.8.0

# postgresql support for Python
psycopg2-binary>=2.9.7


# for getting env
django-environ==0.10.0

# python linter
ruff==0.5.3

# for download file as excel
openpyxl==3.1.2

# for generating pdf
weasyprint==68.1


# as name defined - for git pre-commit
pre-commit==3.3.3


redis==4.4.4

# Freeze time for tests
freezegun==1.2.2

# celery for running jobs
celery==5.3.4
# run task on schedule
django-celery-beat==2.5.0

# zstd decompression for static dumps
zstandard==0.23.0

djangorestframework-api-key==3.0.0

# for serving static files
whitenoise==6.6.0

# Bypasses ALLOWED_HOSTS for the purposes of load balancers
django-lb-health-check==1.0.1

requests

# Image processing
Pillow>=10.0.0

# Async task queue (optional, only needed for RECEIPT_PROCESSING_MODE=async)
celery>=5.3.0
redis>=5.0.0

# AI providers (optional, only needed when using real providers)
openai>=1.0.0
anthropic>=0.7.0
